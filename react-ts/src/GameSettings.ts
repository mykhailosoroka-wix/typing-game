class GameSettings {
    startTime: number;
    timeAdded: number;

    constructor(startTime: number, timeAdded: number) {
        this.startTime = startTime;
        this.timeAdded = timeAdded;
    }
}

export default GameSettings;